{
  "name": "value-api",
  "version": "1.0.0",
  "description": "A simple API that returns a configurable value between 0.0 and 1.0",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel"
  }
}
